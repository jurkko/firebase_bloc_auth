name: auth_firebase_bloc
description: A new Flutter application.

publish_to:

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase: ^7.3.0
  firebase_auth: ^0.18.1+1
  equatable: ^1.2.5
  bloc: ^6.0.3
  flutter_bloc: ^6.0.5
  formz: ^0.3.1


  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
